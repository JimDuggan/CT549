% We can change this and then use the value in the ode function
global param_k;
global outside_temp;

% Create sine wave data
outside_temp = create_sine_wave(0,48,10,15,24);

rng(100);
% resample to add some noise
outside_temp = add_noise_normal(outside_temp,1);










